# BuddyBridge Deep 真机测试清单 —「读知识库」修复

> 修复内容：DSH ACP 会话此前缺少文件访问工具（file/command 工具全部返回 "unknown tool"），
> 导致 AI 无法读写/搜索 llmwiki 知识库。本次在 `src/backend/setup.ts` 的 `buildCordisYml` 中
> 追加了 3 个 in-box 插件条目：`subprocess`（本地子进程运行时）、`tool-fs`（读/写/编辑文件）、
> `tool-fs-search`（glob/grep 搜索，内置 ripgrep），按顺序排在 `acp-demo` 之前。
> 这些插件通过 DSH 的扁平 fallback 目录解析，**无需改 package.json、无需额外安装**。

- 改动文件：`src/backend/setup.ts`、`tests/setup.test.ts`
- 部署产物：`main.js`、`manifest.json`、`styles.css`（复制到插件目录即可）
- 版本：v1.1 / 2026-09-23

---

## 0. 前置准备

- [ ] 用「一键配置后端」按钮重新配置后端（**不要**手工改任何 cordis.yml / 手动跑命令）
- [ ] 确认 `~/.dsh/profiles/acp/cordis.yml` 已由插件重新生成（时间戳为本次配置时间）
- [ ] 已配置 DeepSeek API Key（设置 → 后端 → API Key）
- [ ] 知识库目录 `llmwiki`（Vault 内的笔记文件夹）存在且内含若干 `.md` 文件
- [ ] 版本确认：插件 `0.3.1`（manifest.json）

## 1. cordis.yml 落盘正确

打开 `~/.dsh/profiles/acp/cordis.yml`，确认包含且顺序正确：

- [ ] `- id: subprocess` → `@deepseek-ai/dsh-subprocess-local`
- [ ] `- id: tool-fs` → `@deepseek-ai/dsh-tool-fs`
- [ ] `- id: tool-fs-search` → `@deepseek-ai/dsh-tool-fs-search`（其下 `sampleOverCapGlobResults: false`）
- [ ] 上述 3 条位于 `acp-demo` **之前**（工具须先于 acp-demo 挂载）
- [ ] 原有条目仍在：`llm-deepseek`、`system-prompt`、`agent-instructions`、`sandbox-policy`、`approval`、`fs-sandbox`、`acp-demo`

## 2. 连接与启动

- [ ] 点击「连接」后状态显示已连接（无 "unknown tool" 类错误）
- [ ] 后端进程存活（不闪退），仅可能出现 SQLite experimental 警告（可忽略）
- [ ] 启动日志中无 `assertEntriesActivated` / "entry did not activate" 报错

## 3. 读知识库（核心）

- [ ] 发送：`请读取 llmwiki/ 下的 <某文件>.md 并总结其内容`
- [ ] AI 能列出/定位到该文件（说明 `tool-fs` + `tool-fs-search` 已生效）
- [ ] AI 返回文件真实内容（非 "我无法访问文件"）
- [ ] 无 "unknown tool" / "session 缺少文件访问工具" 报错

## 4. 搜索知识库

- [ ] 发送：`在 llmwiki 里搜索包含「<某关键词>」的笔记`
- [ ] AI 使用 glob/grep 定位到相关文件并给出路径
- [ ] 结果文件路径正确（能对上 Vault 里真实存在的文件）

## 5. 管理知识库（写/编辑）

- [ ] 发送：`在 llmwiki/ 下新建一个测试文件 test.md，内容为 xxx`
- [ ] 文件真实出现在 Vault 对应目录（写权限来自 sandbox-policy 的 workspace-write）
- [ ] 发送编辑指令能修改既有文件
- [ ] （注意）涉及权限/越界写时，审批策略仍按 `approval` policy 弹出确认

## 6. 回归

- [ ] 普通对话仍可正常流式回复 + 思考过程块
- [ ] 会话管理（新建/删除/切换）不受影响
- [ ] 其他既有工具（goals/jobs/skills/bash）仍可用，未因新增 3 条目而失效

## 结果记录

| 项 | 结果（✅/❌） | 备注 |
|----|--------------|------|
| 0 前置 |  |  |
| 1 cordis.yml 落盘 |  |  |
| 2 连接与启动 |  |  |
| 3 读知识库 |  |  |
| 4 搜索知识库 |  |  |
| 5 管理知识库 |  |  |
| 6 回归 |  |  |
